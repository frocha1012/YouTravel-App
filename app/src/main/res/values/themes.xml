<resources>
    <style name="YouTravelLight" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:colorBackground">@color/background</item>
        <item name="colorPrimary">@color/buttonColorPrimary</item>
        <item name="android:textColorPrimary">@color/textColorPrimary</item>
        <item name="android:textColorSecondary">@color/buttonTextPrimary</item>
        <item name="android:statusBarColor">@color/background</item>
        <!-- Ensure navigation bar, if used, and other components are themed appropriately -->
    </style>
</resources>
